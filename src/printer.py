import json

class Printer:
    stars=''
    adress=''

    def __init__(self, name) -> None:
        self.name = name
        
        pass
    def print():
        jsonStr = ''
        parsed = json.loads(jsonStr)

        print(json.dumps(parsed, indent=4, sort_keys=True))

